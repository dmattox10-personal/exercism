/**
 * Created by dmattox10 on 3/15/17.
 */
public class Acronym {

    public static String generate(String phrase)
    {
        String first = Character.toString(phrase.charAt(0));
        for (int i = 0; i < phrase.length(); i++) {
            if (phrase.charAt(i) == " ")
            {

            Character.toString(phrase.charAt(i - 1));
        }
    }
    }
}
